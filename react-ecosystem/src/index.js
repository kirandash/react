import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';

ReactDOM.render(<App/>, document.getElementById('root')); // Finding the root div on index.html file and rendering App component inside that using ReactDOM render method